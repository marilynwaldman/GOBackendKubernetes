
    apiVersion: v1
    kind: Service
    metadata:
      name: be-srv
      labels:
        type: be-type
    spec:
      type: LoadBalancer
      ports:
      - name: be
        port: 80
        protocol: TCP
        targetPort: 3000
      selector:
        type: be-type